# experiments

Raw working output from the development session — filter trials, interpolation
variants, stabilisation sweeps, model comparisons.

Superseded by the finished renders in `../out/`. Kept only as evidence behind
`../docs/findings.md`, which records what each of these showed and why it was
rejected. Nothing here is needed to run the pipeline.

Safe to delete once you trust the conclusions. ~4.6GB.
